extends ../layout

block content
    for item in blog
        .item
            .id=item.id
            .name=item.name
            .author=item.author
            .date=item.date
            .content=item.content